-- Displays file paths and context breadcrumbs in the status line
local options = {
  attach_navic = false,
  theme = "catppuccin-mocha"
}

return {
  "utilyre/barbecue.nvim",
  name = "barbecue",
  opts = options
}
